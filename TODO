* add copyright/license
* display JS surrogate pair chars for > xFFFF
* process query string:
  * nnnn       => decimal codepoint
  * xXXXX      => hex codepoint
  * uXXXXuXXXX => hex surrogate pair
  * q=...      => search query
* display encoded bytes as hex (UTF-8, UTF-16L/E, UTF-32L/E)
* accept encoded hex bytes and decode to char
* display search results in a persistent, scrolling list
* make next/prev buttons repeat when held down
* support scroll wheel in char-input box
* allow user font selections
* add "Fork me" link

* Anomalous displays:
  * Hangul Syllable, First displayed, but rest not because no descriptions
