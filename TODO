* move info table to top with huge char on left
* links to PDFs
* fly-out code-chart menu
* support /regex/ searches
* display JS surrogate pair chars for > xFFFF
* process query string:
  * nnnn       => decimal codepoint
  * xXXXX      => hex codepoint
  * uXXXXuXXXX => hex surrogate pair
  * q=...      => search query
* add UTF-8 bytes support
* display search results in a persistent list
* make next/prev buttons repeat when held down
